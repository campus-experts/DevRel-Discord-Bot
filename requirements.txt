# Core Discord library
discord.py>=2.3.2

# Google API client – YouTube Data API v3
google-api-python-client>=2.118.0

# RSS / Atom feed parser – used to read the GitHub Blog
feedparser>=6.0.11

# YAML config parsing
PyYAML>=6.0.1

# .env file loader
python-dotenv>=1.0.1

# Async HTTP (used transitively by discord.py; pinned for reproducibility)
aiohttp>=3.9.3
